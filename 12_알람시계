H, M = map(int, input().split())

if M >= 45:
    # M 이 45 이상이면 그냥 빼면 됨
    print(H, (M - 45))

elif (M < 45) and (H > 0):
    # M 이 45 미만이면 H - 1 부터 하고 M 결정
    
    # M 이 30 이면 - 45 분 했을 때 45 분으로 출력되어야 함
    # M 이 35 면 - 45 분 했을 때 40 분으로 출력 되어야 함
    # M 이 40 이면 - 45 분 했을 때 55 분 로 출력되어야 함
    
    # 위에 있는 것들로 규칙을 찾으면
    # (M + 15) 를 하면 원하는 시간이 출력 된다는 것을 알 수 있음
    print((H - 1), (M + 15))

else:
    # 시간이 0 시간 이면 23 을 입력해야 하기 때문에 23 가 (M + 15) 를 출력한다
    print(23, (M + 15))
