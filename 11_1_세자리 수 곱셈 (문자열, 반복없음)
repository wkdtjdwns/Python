A = int(input())
B = input() # B 변수를 자료형으로 내버려 둬서 각각의 숫자를 곱셈 할 수 있음

for i in range(3, 0, -1): # 1의 자리 수부터 100의 자리 수 까지 곱하게 함 
    # i = 3 이면 B[2] 즉, B의 1의 자리 숫자와 A 를 곱한다
    # i = 2 이면 B[1] 즉, B의 10의 자리 숫자와 A 를 곱한다
    # i = 1 이면 B[0] 즉, B의 100의 자리 숫자와 A 를 곱한다
    print(A * int(B[i - 1])) 

print(A * int(B))
