# 조건문 if
# 사용법

# if (조건식1):
#   조건식1이 참이면 실행할 코드
# elif (조건식2): // elif는 없어도 되고 무한대로 생성이 가능하다
#   조건식2가 참이면 실행할 코드
# else:
#   위에 있는 모든 조건식이 거짓일 때 실행할 코드

# 나머지 연산자의 중요성

# 0. 사용자 입력을 숫자로 받기 
# 1. 1부터 입력수 까지 출력 => for 문 필요
# 2. 3, 6, 9 인지 판단하여 X 로 변환
n = int(input('숫자를 입력해주세요>>> '))

for i in range(1, n+1):
# 3의 배수면 3으로 나누면 나머지가 0
  if i % 3 == 0: # i % 3 == 0: => i 나누기 3 의 나머지가 0 일때 => i 가 3 의 배수일 때 
    print('X', end = ' ')
  else:
    print(i, end = ' ')
