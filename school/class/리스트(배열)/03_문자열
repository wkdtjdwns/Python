# 리스트에 관해서.. 문자열 
# String = 문자열
str = 'hello world' # ['h', 'e', 'l' .....] 로 저장되고 있음 -> 리스트처럼 저장됨
print(str[2]) # str 의 3 번째 문자를 출력 
print(len(str)) # len() = 문자의 길이를 출력함

list = [1, 2, 3, 4, 5]
print(len(list)) # 배열의 길이도 출력 가능
