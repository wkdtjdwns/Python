# 반복문 - 반복된 작업을 쉽게
# 상황 - 리스트[0, 0, 0, 0, 0, 0.......] => 100칸 생성
# list = [0, 0, ....... 0] 또는
# list = []  list.append(0) 추가하기 100번

# 반복 명령어 for, while
# for = 제한된 횟수 만큼만 반복
# k = 반복하면서 사용할 변수
# range(범위) = 0 ~ (범위 - 1) 까지 
for k in range(2, 5): # 2 부터 (5 - 1) 까지
# 파이썬은 { } <= 중괄호 대신 들여쓰기(Tab 키) 로 대체 함   
# : = 중괄호 시작과 같은 뜻
  # 안에 들여쓰기를 했음
  print(k)
  print('반복중')
