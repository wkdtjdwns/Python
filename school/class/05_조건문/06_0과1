# 1을 입력하면 0 출력
# 0을 입력하면 1 출력

# 조건문 사용
n = input() # 문자열로 입력

# if n == 1 #n 은 문자값이므로   문자 1과 숫자 1은 다름
if n == '1': # 문자 '1'
  print('0') # 문자 0 은 계산 불가능

elif n == '0':
  print('1')

# 또 다른 방법
n = int(input())

print((1 - n))
