# 사용자로부터 "-1"을 입력받아야만 "프로그램 종료"를 출력함

a = int(input("프로그램을 종료하려면 -1 을 입력해주세요"))

while a != -1: # while 의 조건식이 "True" 일 때 명령문을 반복함
    a = int(input("프로그램을 종료하려면 -1 을 입력해주세요")) # break = 가장 가까운 반복문을 탈출함 (무한반복 X)

print("프로그램 종료")
