# 매개변수 없는 함수
# def 함수이름 (): 
#   명령블록
#   ... 
#   return 리턴값

# 사용법 (1 부터 10 사이의 숫자 중 랜덤 숫자 출력) 

import random # random 함수는 import 해줘야만 사용 가능 

def getRandomNumber ():
    number = random.randint(1, 10) # random.randint(숫자1, 숫자2) = 숫자1 과 숫자2 사이의 숫자 중 랜덤 값
    return number

print(getRandomNumber())
