n = int(input())  # 과목 수

t = list(map(int, input().split())) # 시험 점수

m = max(t) # 시험 최고점

list = [] # 새로운 점수 저장을 위한 리스트
for i in t :
    list.append(i / m *100)  # 새로운 점수 생성

avg = sum(list) / n # sum() => 괄호 안의 값을 모두 더해줌

print(avg)
